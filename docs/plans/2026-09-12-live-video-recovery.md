# Live video status and recovery

Reported: native preview is black despite “Your camera is broadcasting.”

Confirmed defect: the old headline checked only capturer startup, not frame arrival. SDK source confirms startup is a counter, so it cannot establish a working video feed. The original representable/grid did display synthetic green/magenta frames in the simulator, so no physical black-screen root cause is established.

Implementation: a session-owned frame probe checks recent camera frames without storing images and without relying on whether the preview is onscreen. Official SwiftUIVideoView provides per-tile frame status; stable track identity retains the right state across participant changes. Stalled video shows a readable waiting overlay and Restart camera or Reconnect video. Refresh picture recreates the display using the alternate supported renderer without stopping the media track. Connection text says Connected to team rather than claiming live media. Membership checks, grants, microphone restrictions and stored scores are unchanged.

Verification: actual synthetic publisher/viewer transport passed with received video frames, viewer publishing rejected and microphone publishing rejected. Native original renderer displayed real synthetic frames. [runtime-tested] Final synthetic recovery test passed: real colored frames, Refresh picture alternate renderer, stopped feed with readable scrim and named Restart camera, then resumed frames. Screenshots independently inspected at /tmp/ba4l-render-verified-shots/. Fixed both overlay contrast and overly broad accessibility label found during testing. Native 36 contrast pairs and 10 prominent-button guards passed. Frame-probe lifecycle independently reviewed. Physical iPhone/iPad devices unavailable to this Mac; user was asked to connect/unlock a device and clarify whether remote viewers were also black. No response yet. A real camera and second-device test remains required before calling the reported livestream issue fixed.
