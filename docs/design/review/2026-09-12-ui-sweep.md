# BA4L UI sweep and refinement

User report: native homepage in Dark Mode has unreadable Live Lane card, inconsistent surfaces, and a black bottom strip. Sweep every route before patching shared styles. Preserve the approved forest/ivory/muted-gold identity, native navigation, real data and all existing workflows.

## Initial evidence and plan

Native source inventory covers Tonight, Score, League/Records/Career, Review, Account/Sign-in/team access, Season/week detail/scorecards, match setup/insights, live lane/shared viewing/replay, device archive, scan and voice sheets. Most use adaptive system Forms/Lists and semantic text. The definite repeated failure is fixed BrandForest foreground over adaptive BrandGoldSurface (two homepage live cards, dark contrast 1.14:1). The homepage extends its background only through the top safe area, leaving the bottom system canvas black. Dark BrandGreen uses bright lime across actions despite the accepted design limiting lime to the mark.

After completing the sweep:
1. Pair gold surface with an adaptive foreground asset and validate all appearance/contrast variants.
2. Give Tonight a full safe-area background, clearer elevated card hierarchy in Dark Mode and muted gold accent actions.
3. Prioritize current results before idle camera discovery. Keep Live Lane obvious without letting its empty state displace current scores. Avoid showing a nonparticipant's empty score as a real pre-bowl result.
4. Fix additional demonstrated responsive/contrast problems across web/native from the sweep.
5. Verify phone/iPad, light/dark, larger text, bottom-scroll clearance, route navigation and browser layout screenshots. Add a durable contrast regression for theme pairs.
6. Build and release the verified refinement to web and TestFlight using the existing authorized workflow.

Example pairing: `foregroundStyle(Color("OnGoldSurface"))` with `background(Color("BrandGoldSurface"))`. Full-bleed background ignores insets; content continues using native scroll safe areas, without fixed tab-bar padding.

## Completed sweep and fixes

Native inventory: 31 SwiftUI views across 17 files, covering all app sections and supporting sheets. Confirmed recurring issues: two forest-on-dark-gold homepage cards (1.14:1), seven unpaired prominent button labels, faint explicit secondary labels on ivory, top-only homepage canvas, and lime action tint inconsistent with the accepted palette. Fixed paired tokens, full-bleed background, elevated dark cards, gold actions and explicit secondary labels in 13 native view files. Results now precede idle camera information, the redundant calendar backdrop is removed, and a selected nonparticipant no longer shows a misleading pre-bowl zero as their active result.

Web inventory and results are in docs/plans/2026-09-12-web-route-audit.md. 63 baseline cases and 21 post-fix cases passed; 80 unit tests /61,294 assertions and production build passed. Four visual issues fixed: two legacy branding surfaces, avatar contrast and secondary action colors. Web code d6560a3 published to Vercel dpl_6A7mLZgL8ZSsHJpGw2JYtRvwr4KV, READY at bigals4life.com. [runtime-tested] visual flow; production standings route loaded.

Native six broad simulator flows passed: phone/iPad, light/default, dark/default, dark accessibility XXXL. Exercised Tonight/Season/Live/Score/League/Records/Review/Account/TeamAccess. All-weeks final action is hittable above native collapsed navigation; root inspected actual bottom screenshot. Native Form/List behavior retained. Twenty-eight explicit theme pairs and eight secondary/native surface pairs pass4.5:1; nine prominent callsites enforce explicit foregrounds. Root reviewed the guard and independently mutated each of nine callsites; all omissions were rejected. These tests do not certify every system-rendered label. Account profile/discovery tests passed. Final signed build10 archive passed.

A separate authentication routing bug was discovered in nonmember waiting-page coverage. Its isolated43-case fix is prepared, not applied/published pending Doug’s explicit review required by AGENTS.md. See docs/plans/2026-09-12-waiting-access-review.md. This does not block the visual-only release.
